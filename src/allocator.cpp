// =============================================================================
// allocator.cpp — translation unit reserved for the front-door Heisenheap
// class implementation.
// =============================================================================
//
// CMake requires `add_library(... SOURCES)` to have at least one .cpp file
// to compile, so we keep this empty TU around to satisfy that. The
// library's symbols currently come from src/error.cpp and src/options.cpp.
// =============================================================================
